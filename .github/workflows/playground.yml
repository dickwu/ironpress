name: Deploy Playground

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'playground/**'
      - 'tests/parity/**'
      - 'tests/parity_support/**'
      - 'tests/feature_parity.rs'
      - 'scripts/parity*.sh'
      - 'Cargo.toml'
      - '.github/workflows/playground.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/wasm-pack
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: playground-${{ runner.os }}-${{ hashFiles('Cargo.toml') }}

      - name: Install build tools
        run: |
          command -v wasm-pack || cargo install wasm-pack
          sudo apt-get update -qq
          sudo apt-get install -y -qq poppler-utils fonts-liberation fonts-dejavu-core fonts-noto-cjk
          mkdir -p "$HOME/.local/share/fonts"
          cp tests/parity/fonts/Parity*.ttf "$HOME/.local/share/fonts/"
          fc-cache -f

      - name: Build playground
        run: |
          wasm-pack build --target web --features wasm --no-default-features
          cp pkg/ironpress.js playground/
          cp pkg/ironpress_bg.wasm playground/

      - name: Build feature parity report
        env:
          FONTCONFIG_FILE: ${{ github.workspace }}/tests/parity/fonts/fonts.conf
        run: scripts/parity.sh

      - name: Stage feature parity report
        run: |
          mkdir -p playground/parity
          cp -R tests/parity/reports playground/parity/
          cp -R tests/parity/refs playground/parity/
          cp -R tests/parity/out playground/parity/

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: playground
      - id: deployment
        uses: actions/deploy-pages@v4
